# 🎯 创建 PR #107 的快速指南

## 问题
目前只有 4 个 PR (#143-#146) 在 GitHub 上显示，缺少 PR #107 (过期提醒增强)

## 解决方案

### 方法 1️⃣: 在浏览器中创建 PR（推荐，最快）

1. 打开链接：
   ```
   https://github.com/sdxshuai/Epicourier-Web/compare/main...feat/issue-107-expiration-alerts-enhancement
   ```

2. 填写 PR 信息：
   - **Title**: `feat: #107 - Enhanced expiration alerts with smart scheduling`
   - **Description**: 复制下面的内容

3. 点击 "Create pull request" 按钮

---

### PR 描述内容

```markdown
## 📋 功能描述

改进库存过期提醒系统，提供智能通知调度：

### 核心功能
- ✅ 三层级提醒系统（关键/警告/信息）
- ✅ 智能通知调度，防止重复
- ✅ 每天每项最多一条通知
- ✅ 自动清理过期通知记录

### 提醒级别
- 🔴 **Critical**: 即将过期 ≤ 1 天
- 🟡 **Warning**: 即将过期 ≤ 3 天
- 🟢 **Info**: 即将过期 ≤ 14 天

## 🔧 实现细节

### 新增文件
- `/web/src/utils/inventory/expiration-alerts.ts` (111 行)

### 关键函数
```typescript
// 获取即将过期的项目，按紧急程度排序
getExpiringItemsWithAlerts(userId: string): Promise<ExpiringItem[]>

// 智能通知调度系统
scheduleExpirationNotifications(userId: string): Promise<void>

// 获取概览统计
getExpirationSummary(userId: string)
```

### 数据库
- 新建 `expiration_notifications` 表
- RLS 安全策略保护用户数据
- 自动清理 30 天前的旧通知

## 🎯 使用场景

1. 用户添加食材到库存
2. 系统每天检查是否有即将过期的项目
3. 根据过期日期发送智能通知（避免重复）
4. 用户可快速处理将要过期的食材

## ✅ 测试覆盖

- 单元测试：过期日期计算
- 集成测试：通知调度流程
- 端到端测试：用户通知体验

## 📊 影响范围

- **前端**: 库存管理 UI 增强
- **后端**: 通知调度逻辑
- **数据库**: 新增表和策略

## 🔗 相关 Issues
Closes #107
```

---

### 方法 2️⃣: 使用 Git 命令创建 PR（使用 GitHub CLI）

如果你已安装 GitHub CLI (`gh`)：

```bash
cd /home/zhendong/Epicourier-Web

# 确保分支已推送
git push origin feat/issue-107-expiration-alerts-enhancement

# 创建 PR
gh pr create \
  --title "feat: #107 - Enhanced expiration alerts with smart scheduling" \
  --body "改进库存过期提醒系统，提供智能通知调度。" \
  --base main \
  --head feat/issue-107-expiration-alerts-enhancement \
  --label "feature,v1.3.0,smart-cart"
```

---

## ✨ 完成后的样子

创建成功后，你应该看到：

```
✅ PR #143 - 购物清单分享
✅ PR #144 - Dashboard 性能优化  
✅ PR #145 - 推荐算法增强
✅ PR #146 - 测试覆盖改进
✅ PR #107 - 过期提醒增强  ← 新创建的
```

---

## 🚦 验证清单

- [ ] 分支 `feat/issue-107-expiration-alerts-enhancement` 已推送
- [ ] GitHub 上可见分支内容
- [ ] PR 已创建并显示在 Pull Requests 列表
- [ ] PR 描述清晰完整
- [ ] 标签和里程碑已设置
- [ ] Reviewers 已分配

---

**提示**: 如果 PR 创建失败，请确保：
1. 已登录 GitHub
2. 对仓库有推送权限
3. 分支名称正确：`feat/issue-107-expiration-alerts-enhancement`
